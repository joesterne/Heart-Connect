sed -i '/val gesturesEnabled = currentRoute != "auth"/a \    val isGlobalLoading by viewModel.isGlobalLoading.collectAsState()\n    Box(modifier = Modifier.fillMaxSize()) {' app/src/main/java/com/example/ui/navigation/AppNavigation.kt
sed -i 's/^}$/    }\n    if (isGlobalLoading) {\n        Box(\n            modifier = Modifier\n                .fillMaxSize()\n                .background(MaterialTheme.colorScheme.scrim.copy(alpha = 0.3f)),\n            contentAlignment = Alignment.Center\n        ) {\n            CircularProgressIndicator()\n        }\n    }\n}/' app/src/main/java/com/example/ui/navigation/AppNavigation.kt
